{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "notion-task-id-picker",
  "title": "Notion Task ID Picker",
  "description": "Pick the task ID from Notion tasks.",
  "icon": "extension-icon.png",
  "author": "inady",
  "platforms": [
    "macOS",
    "Windows"
  ],
  "categories": [
    "Productivity"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "pick-a-notion-task-id",
      "title": "Pick a Notion Task ID",
      "description": "Search and select a task to paste or copy its ID",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "notionApiToken",
      "type": "password",
      "required": true,
      "title": "Notion API Token",
      "description": "Your Notion integration API token (starts with secret_)"
    },
    {
      "name": "taskDatabaseId",
      "type": "textfield",
      "required": true,
      "title": "Task Database ID",
      "description": "The ID of your Notion task database (32-character hex string)"
    },
    {
      "name": "sprintDatabaseId",
      "type": "textfield",
      "required": true,
      "title": "Sprint Database ID",
      "description": "The ID of your Notion sprint database (32-character hex string)"
    },
    {
      "name": "taskIdPropertyName",
      "type": "textfield",
      "required": true,
      "title": "Task ID Property Name",
      "description": "The name of the Task ID property in your task database",
      "default": "Task ID"
    },
    {
      "name": "taskStatusPropertyName",
      "type": "textfield",
      "required": true,
      "title": "Task Status Property Name",
      "description": "The name of the Status property in your task database",
      "default": "Status"
    },
    {
      "name": "sprintRelationPropertyName",
      "type": "textfield",
      "required": true,
      "title": "Sprint Relation Property Name",
      "description": "The name of the Sprint relation property in your task database",
      "default": "Sprint"
    },
    {
      "name": "sprintStatusPropertyName",
      "type": "textfield",
      "required": true,
      "title": "Sprint Status Property Name",
      "description": "The name of the Status property in your sprint database",
      "default": "Status"
    }
  ],
  "dependencies": {
    "@notionhq/client": "^2.3.0",
    "@raycast/api": "^1.103.10",
    "@raycast/utils": "^1.17.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
